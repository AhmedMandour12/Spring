<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">


<hibernate-configuration>

	<session-factory>
		<!-- JDBC Database connection settings -->
		<property name="connection.driver_class">oracle.jdbc.OracleDriver</property>
		<property name="connection.url">jdbc:oracle:thin:@//localhost:1521/orclpdb</property>
		<property name="connection.username">hibernate</property>
		<property name="connection.password">123</property>
		<property name="current_session_context_class">thread</property>
		<property name="hbm2ddl.auto">update</property>


		<!-- show sql query -->
		<property name="show_sql">true</property>
		
		
		<!-- validate -->
		<!-- Check only (match entities), no CREATE, no UPDATE, no DROP -->

		<!-- create -->
		<!-- DROP + CREATE -->

		<!-- update -->
		<!-- Update/modify schema to match entities -->

		<!-- create-drop -->
		<!-- DROP + CREATE at startup, then DROP at shutdown -->

	</session-factory>

</hibernate-configuration>